<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.market.dao.RecipeMapper">

<!-- 레시피 게시판 조회 -->
<select id="getRecipeList" resultType="Recipe">
	select 
		recipe_num as recipeNum,
		user_num as userNum,
	    title as title,
	    score as score,
	    to_char(indate, 'YYYY-MM-DD') as indate
	from
	    recipe_table
	order by
		recipe_num desc
</select>

<!-- 레시피 검색 -->
<select id="searchRecipe" parameterType="String" resultType="Recipe">
	select
		recipe_num as recipeNum,
		user_num as userNum,
		title as title,
		score as score,
		to_char(indate, 'YYYY-MM-DD') as indate
	from
		recipe_table
	where
		title like '%'||#{searchword }||'%'
	order by
		recipe_num desc
</select>
</mapper>